# Docker Compose Service Restart

This directory includes tools to easily restart the CoolBeans services (e.g. in case of switching off the device hosting services, etc.)

## Quick Start

### Bash Script (Direct)
```bash
cd /path/to/webapp
./restart-services.sh
```
