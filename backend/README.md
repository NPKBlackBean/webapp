## CoolBeans Web App
### Backend
To run fastapi server locally run:
```commandline
cd backend
uv run fastapi dev app/main.py --host 0.0.0.0
```

To run tests use:
```commandline
cd backend
uv run pytest
```

